"use client";
import Link from "next/link";
import { useState } from 'react';
import { supabase } from '@/lib/supabase/browser';
export default function SetPassword(){
 const [password,setPassword]=useState('');const [confirm,setConfirm]=useState('');const [busy,setBusy]=useState(false);const [error,setError]=useState('');const [done,setDone]=useState(false);
 return <main className="flex min-h-screen items-center justify-center bg-slate-950 p-4"><form className="w-full max-w-md space-y-4 rounded-2xl bg-white p-8 text-slate-900" onSubmit={async e=>{e.preventDefault();if(!supabase||busy)return;if(password!==confirm){setError('Passwords do not match.');return;}setBusy(true);setError('');try{const {data}=await supabase.auth.getUser();if(!data.user)throw Error('This link is expired or invalid. Request a new reset link.');const {error}=await supabase.auth.updateUser({password});if(error)throw error;await supabase.auth.signOut();setDone(true);setPassword('');setConfirm('');}catch(e){setError(e instanceof Error?e.message:'Unable to set password.');}finally{setBusy(false);}}}><h1 className="text-2xl font-bold">{done?'Password saved':'Choose your password'}</h1>{done?<Link href="/" className="text-indigo-600">Continue to sign in</Link>:<><p className="text-sm text-slate-500">Use at least 12 characters. This also completes your invitation.</p><label className="block">New password<input required minLength={12} maxLength={128} autoComplete="new-password" type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-1 w-full rounded-lg border p-3"/></label><label className="block">Confirm password<input required minLength={12} autoComplete="new-password" type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} className="mt-1 w-full rounded-lg border p-3"/></label>{error&&<p role="alert">{error}</p>}<button disabled={busy} className="w-full rounded-lg bg-indigo-600 p-3 font-semibold text-white disabled:opacity-50">{busy?'Saving…':'Save password'}</button><Link href="/auth/forgot-password" className="block text-sm text-indigo-600">Request another reset link</Link></>}</form></main>;
}
